use aoc_2020::prelude::*;

fn main() -> Result<()> {
    Ok(())
}

enum Hue {
    Red,
    Orange,
    White,
    Yellow,
    Gold,
    Olive,
    Plum,
    Blue,
    Black,
}

struct Bag {}

struct Rule {}
